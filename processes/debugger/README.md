It is an optional process which can be activated or deactived

It injects itself into the context.
It uses the logger to show all modifications to the state
it allows to pause or continue the pipeline for specific time/condition/iife fn to be true
it allows to show all atomic modifications, from all history tracking systems

It can optionally connect to the CLI activated or deactivated as well
The cli process allows it to perform specific task live during runtime (instead of predefined in pipeline )
