It automatically injects itself into the context of the pipeline

it is used to pass prameters to specific pieplines when time comes

as well as to show outputs (resulting pipeline context/state) to specific piplines

it can be used directly from command line by running the CLI.js (which will also run the entire codebase too)
it uses standerds of any other cli using oclif,chalk,boxen,ora and inquirer
