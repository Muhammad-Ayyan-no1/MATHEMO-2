This process takes a single pipeline and runs it
It assumes each pipeline contains a hashmap (OBJECT) containing data such as ID, CBK (Code base keywords) for taskName (process to spawn), a prams sub-hashmap (object as a child) which may contain anything, a keyword for specific apiMethod (sub-process) also an CBK
