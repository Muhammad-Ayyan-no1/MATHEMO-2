This process provides polyfills for depdencies (librarys)

it is also injected into context as a hashmap

This system provides a consistent API to use any libs, instead of importing libs directly, this process should be used, which automatically polyfill if needed or route to library (if library works & installed)
